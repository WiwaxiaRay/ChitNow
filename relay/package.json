{
  "name": "chitnow-relay",
  "version": "1.0.0",
  "private": true,
  "description": "ChitNow APNs relay Worker — sends generic push notifications via Apple APNs",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250430.0",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4",
    "wrangler": "^4.13.2"
  }
}
